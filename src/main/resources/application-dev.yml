server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/saju
    username: saju
    password: ${MYSQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
